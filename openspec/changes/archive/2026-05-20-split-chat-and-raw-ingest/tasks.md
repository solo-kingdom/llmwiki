## 1. 导航与路由重构（Chat / Ingest 分离）

- [x] 1.1 更新 `WorkbenchView` 类型与路由解析：新增 `chat` 与 `ingest` 的明确映射关系
- [x] 1.2 顶部导航文案改为 `Chat`、`Ingest`，并保持 `Jobs/Timeline/Logs/Settings`
- [x] 1.3 保持 `/` 默认进入 Chat，同时新增 Raw Ingest 对应路径（如 `/ingest`）
- [x] 1.4 兼容现有历史路径与刷新恢复逻辑，避免直接打开深链时落错页

## 2. 新建 Raw Ingest 页面

- [x] 2.1 新增 `IngestRaw`（或同义命名）页面组件骨架，接入 `WorkbenchLayout`
- [x] 2.2 文件输入区：支持按钮上传与拖放上传，展示已选择文件列表
- [x] 2.3 文本块输入区：支持新增/删除文本块，块内含标题（可选）和正文（必填）
- [x] 2.4 提交参数区：支持批次标题/来源（可选）
- [x] 2.5 主按钮 `直接归档`：无输入时禁用并给出提示

## 3. 提交编排与状态反馈

- [x] 3.1 实现文本块拼接函数：将多个文本块合成为单份 markdown（含块标题分隔）
- [x] 3.2 文本提交复用 `createTextIngestJob`（通过 AppContext 封装）
- [x] 3.3 文件提交复用 `uploadIngestJobs`（批量）
- [x] 3.4 混合提交时统一汇总反馈：accepted/rejected、job id 列表、失败原因
- [x] 3.5 提交完成后提供“查看 Jobs”跳转动作，并刷新任务列表

## 4. Chat 页面与文案一致性

- [x] 4.1 将原 Ingest 聊天入口文案统一替换为 Chat（导航、空态提示、测试期望）
- [x] 4.2 确认 Chat 页能力不回归：会话切换、模型选择、附件上传、归档流程保持可用

## 5. 测试与回归

- [x] 5.1 更新导航与路由相关测试（默认页、路径恢复、按钮文案）
- [x] 5.2 新增 Raw Ingest 页面交互测试（多文本块、文件选择、禁用态、提交反馈）
- [x] 5.3 覆盖混合提交场景测试（文本成功/文件部分失败等）
- [x] 5.4 执行前端测试并修复失败用例

## 6. 文档与可用性收尾

- [x] 6.1 更新 README/Web 工作流描述：区分 Chat 与 Ingest 两类入口
- [x] 6.2 补充短说明文案：何时用 Chat、何时用 Ingest，降低用户选择成本
