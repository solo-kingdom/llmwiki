## 1. Settings 与类型扩展

- [x] 1.1 在后端 settings 响应中新增 `ui_language`、`doc_language`
- [x] 1.2 在 settings 更新白名单中新增上述两个 key
- [x] 1.3 增加语言值校验逻辑（仅 `zh` / `en`）
- [x] 1.4 为空值/历史数据定义默认值回退（默认 `zh`）
- [x] 1.5 更新前端 `Settings` 类型定义并通过编译

## 2. 前端 i18n 首版落地

- [x] 2.1 新建 `zh` / `en` 词典文件与统一 `t(key)` 方法
- [x] 2.2 接入 `ui_language` 到前端翻译上下文（与 AppContext 集成）
- [x] 2.3 替换 `WorkbenchLayout` 导航文案，移除硬编码英文
- [x] 2.4 替换 `SettingsPage` 核心区块标题、按钮、说明文案
- [x] 2.5 替换 `IngestChat` 高频交互文案（状态、按钮、错误提示）
- [ ] 2.6 验证首版核心页面无明显中英混用

## 3. 文档语言注入全部生成路径

- [x] 3.1 抽取统一文档语言解析函数（读取 `doc_language`，回退默认）
- [x] 3.2 在 `pipeline` 的 `analysis/generation` prompt 中注入语言规则
- [x] 3.3 在会话系统提示中注入语言规则（归档链路可感知）
- [x] 3.4 在附件摘要 prompt 中注入语言规则
- [x] 3.5 在 rollback prompt 中注入语言规则
- [x] 3.6 明确"设定语言为主，英文注释"的提示模板（`zh` / `en` 双分支）

## 4. 一致性与可追溯性

- [x] 4.1 设计并实现 `doc_language` 在 job 级别的快照方案（推荐）
- [x] 4.2 若暂不做快照，补充注释与文档说明"执行时配置生效"的行为
- [x] 4.3 检查所有生成入口最终都走统一语言策略（避免分叉）

## 5. 测试与验收

- [x] 5.1 新增/更新 settings API 测试：新增字段、合法值、非法值
- [ ] 5.2 新增前端测试：切换 `ui_language` 后关键文案变化
- [x] 5.3 新增 ingest 测试：不同 `doc_language` 下 prompt 断言
- [x] 5.4 新增回滚/附件摘要语言策略测试
- [ ] 5.5 手工验收：`zh` 与 `en` 两种配置下，归档和普通生成输出语言符合预期

## 6. 文档与发布说明

- [x] 6.1 更新 README 设置章节，说明 `ui_language` / `doc_language`
- [x] 6.2 补充"已知限制"：首版仅覆盖中英文与核心页面文案
- [x] 6.3 在变更记录中注明语言策略及向后兼容行为
